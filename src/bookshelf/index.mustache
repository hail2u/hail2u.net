<!DOCTYPE html>
<html lang="{{lang}}">
	{{> head}}

	<body>
		<main>
			<section>
				<h1>{{title}}</h1>

				<p>以下の本表紙画像はAmazonアソシエイトを利用しています。そのため、各表紙のリンク先はAmazonで、アフィリエイト・リンクになっています。</p>

				{{#books}}
					{{#isFirstInMonth}}
						<section>
							<h2><time datetime="{{strYear}}-{{strMonth}}">{{year}}年{{month}}月</time></h2>

							<ol class="showcase">
					{{/isFirstInMonth}}

					<li>{{{body}}}</li>

					{{#isLastInMonth}}
							</ol>
						</section>
					{{/isLastInMonth}}
				{{/books}}
			</section>
		</main>

		{{> page-footer}}
	</body>
</html>
