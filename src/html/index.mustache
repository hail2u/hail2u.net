<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> global-header}}

    <main class="content">
      <section class="section">
        <h1 class="hang">最近書いた記事</h1>

        <ol class="posts">
          {{#articles}}
            <li>
              <div class="info"><time class="date" datetime="{{html5PubDate}}">{{strPubDate}}</time></div>
              <a class="title" href="{{link}}">{{{title}}}</a>
            </li>
          {{/articles}}
        </ol>
      </section>

      <section class="section pick">
        {{#features}}
          {{#isLatest}}
            <a href="{{link}}">
              <h1>{{title}}</h1>

              <footer>
                <p><time datetime="{{html5PubDate}}">{{strPubDate}}</time></p>
              </footer>
            </a>
          {{/isLatest}}
        {{/features}}
      </section>

      <section class="section">
        <h1 class="hang">ここ以外だと……</h1>

        <p>ちょっとしたことは<a href="https://twitter.com/hail2u_">Twitter</a>、スマホで撮った写真は<a href="https://instagram.com/hail2u_/">Instagram</a>、プログラムとかは<a href="https://github.com/hail2u">GitHub</a>、買ったものやら読んだ本やら気になったウェブページなどは<a href="https://pinboard.in/u:hail2u/">Pinboard</a>でやってます。</p>
      </section>
    </main>

    {{> site-search}}
    {{> global-footer}}
  </body>
</html>
