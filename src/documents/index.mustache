<!DOCTYPE html>
<html lang="{{lang}}" itemscope itemtype="http://schema.org/{{schemaType}}">
  {{> head}}

  <body>
    {{> header}}

    <main>
      {{#documents}}
        {{#isFirstInYear}}
          <section id="on-{{strYear}}">
            <h2><time datetime="{{strYear}}">{{year}}年</time></h2>

            <ol>
        {{/isFirstInYear}}

        <li><a href="{{link}}">{{title}}</a></li>

        {{#isLastInYear}}
            </ol>
          </section>
        {{/isLastInYear}}
      {{/documents}}
    </main>

    {{> footer}}
  </body>
</html>
