<!DOCTYPE html>
<html lang="{{lang}}" itemscope itemtype="http://schema.org/{{schemaType}}">
	{{> head}}

	<body>
		{{> page-header}}

		<main id="main" class="page-content">
			<section>
				<h1>{{title}}</h1>

				<p>これらの文書にはかなり古い（10年以上前）ものもあり、現在はあまり役に立たないかもしれません。更新日時に気をつけて読んでください。</p>

				{{#documents}}
					<section>
						<header>
							<p class="metaline"><time datetime="{{strYear}}-{{strMonth}}-{{strDate}}T{{strHour}}:{{strMinute}}:{{strSecond}}+09:00">{{year}}年{{month}}月{{date}}日</time></p>

							<h2><a href="{{link}}">{{title}}</a></h2>
						</header>

						<p>{{description}}</p>
					</section>
				{{/documents}}
			</section>
		</main>

		{{> page-footer}}
	</body>
</html>
