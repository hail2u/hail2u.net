<!DOCTYPE html>
<html lang="{{lang}}">
	{{> head}}

	<body>
		{{> page-header}}

		<h1>{{title}}</h1>

		<p>これらの文書にはかなり古い（10年以上前）ものもあり、現在はあまり役に立たないかもしれません。公開された日付に気をつけて読んでください。</p>

		{{#documents}}
			<section>
				<p class="date"><time datetime="{{strYear}}-{{strMonth}}-{{strDate}}T{{strHour}}:{{strMinute}}:{{strSecond}}+09:00">{{year}}年{{month}}月{{date}}日</time></p>

				<h2><a href="{{link}}">{{title}}</a></h2>

				<p>{{description}}</p>
			</section>
		{{/documents}}

		{{> page-footer}}
	</body>
</html>
