<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> header}}

    <main>
      {{#documents}}
        {{#isFirstInYear}}
          <section id="on-{{strYear}}">
            <h1><time datetime="{{strYear}}">{{year}}年</time></h1>

            <ol>
        {{/isFirstInYear}}

        <li><a href="{{link}}">{{title}}</a></li>

        {{#isLastInYear}}
            </ol>
          </section>
        {{/isLastInYear}}
      {{/documents}}
    </main>

    {{> footer}}
  </body>
</html>
