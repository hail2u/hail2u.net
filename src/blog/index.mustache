<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> header}}

    <main>
      {{#items}}
        {{#isFirstInYear}}
          <section id="on-{{year}}">
            <h1 class="hang"><time>{{year}}</time></h1>

            <ol class="posts" role="directory">
        {{/isFirstInYear}}

        <li>
          <time datetime="{{html5PubDate}}">{{strPubDate}}</time>
          <a href="{{link}}">{{title}}</a>
        </li>

        {{#isLastInYear}}
            </ol>
          </section>
        {{/isLastInYear}}
      {{/items}}
    </main>

    {{> footer}}
  </body>
</html>
