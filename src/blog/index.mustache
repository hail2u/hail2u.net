<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> header}}

    <main>
      {{#items}}
        {{#isFirstInMonth}}
          <section id="on-{{html5YearMonth}}">
            <h1><time datetime="{{html5YearMonth}}">{{year}}年{{month}}月</time></h1>

            <ol role="directory">
        {{/isFirstInMonth}}

        <li>
          <time datetime="{{html5PubDate}}">{{strPubDate}}</time>
          <a href="{{link}}">{{title}}</a>
        </li>

        {{#isLastInMonth}}
            </ol>
          </section>
        {{/isLastInMonth}}
      {{/items}}
    </main>

    {{> footer}}
  </body>
</html>
