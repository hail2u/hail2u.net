<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> header}}

    <main>
      <section>
        <h1>最新60枚</h1>

        <ul>
          {{#photos}}
            <li><a href="{{url}}"><img alt="{{filename}}" src="{{url}}"></a></li>
          {{/photos}}
        </ul>
      </section>

      <nav>
        <p><a href="{{canonical}}log.html">{{shortTitle}}の全記録</a></p>
      </nav>
    </main>

    {{> footer}}
  </body>
</html>
