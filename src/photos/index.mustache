<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> header}}

    <main>
      <section>
        <ol>
          {{#photofiles}}
            <li><a href="{{url}}"><img alt="{{filename}}" src="{{url}}"></a></li>
          {{/photofiles}}
        </ol>
      </section>
    </main>

    {{> footer}}
  </body>
</html>
