<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> site-menu}}
    {{> page-header}}

    <p>かつて「特集」としていた長めのきっちりと書かれた記事もここに含まれています(URLは変えていません)。すべての記事が並んでいるので、ページ内検索を使って、記事見出しで検索できます。記事本文を含めて検索したい場合は、<a href="https://duckduckgo.com/?q=site%3A{{domain}}">DuckDuckGo</a>のウェブサイト内検索を利用してください。</p>

    <nav>
      <h2>年別</h2>

      <p>{{#articles}}{{#isFirstInYear}}<a href="#{{strYear}}">{{year}}&#x2060;年</a>&#x2003;{{/isFirstInYear}}{{/articles}}</p>
    </nav>

    {{#articles}}
      {{#isFirstInMonth}}
        <section{{#isFirstInYear}} id="{{strYear}}"{{/isFirstInYear}}>
          <h2><time datetime="{{strYear}}-{{strMonth}}">{{year}}年{{month}}月</time></h2>
      {{/isFirstInMonth}}

      <p class="date"><time datetime="{{strYear}}-{{strMonth}}-{{strDate}}T{{strHour}}:{{strMinute}}:{{strSecond}}+09:00">{{year}}年{{month}}月{{date}}日</time></p>

      <p><a href="{{link}}">{{title}}</a></p>

      {{#isLastInMonth}}
        </section>
      {{/isLastInMonth}}
    {{/articles}}

    {{> page-footer}}
  </body>
</html>
