<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> site-menu}}
    {{> page-header}}

    <p>肯定的な理由で気になったかどうかは、おおむね付記で判断できるようにしています。ただし、コメントのないものも多くあります。また、古いリンクの多くは参照先が存在しないかもしれません。修正する手段が存在しないため、そのままになっています。ページ内検索を使って、ウェブページの見出しや付記で検索できます。</p>

    {{#links}}
      {{#isFirstInMonth}}
        <section{{#isFirstInYear}} id="{{strYear}}"{{/isFirstInYear}}>
          <h2><time datetime="{{strYear}}-{{strMonth}}">{{year}}年{{month}}月</time></h2>
      {{/isFirstInMonth}}

      <p class="date"><time datetime="{{strYear}}-{{strMonth}}-{{strDate}}T{{strHour}}:{{strMinute}}:{{strSecond}}+09:00">{{year}}年{{month}}月{{date}}日</time></p>

      <p><a href="{{link}}">{{title}}</a>: {{description}}</p>

      {{#isLastInMonth}}
        </section>
      {{/isLastInMonth}}
    {{/links}}

    {{> page-footer}}
  </body>
</html>
