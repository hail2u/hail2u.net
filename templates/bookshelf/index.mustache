<!DOCTYPE html>
<html lang="{{lang}}">
  {{> head}}

  <body>
    {{> page-header}}

    <nav>
      <p>サムネイルではなく<a href="/bookshelf/list.html">テキスト版</a>を開くと、書名や著者名で検索できます。</p>
    </nav>

    {{#books}}
      {{#isFirstInMonth}}
        <section{{#isFirstInYear}} id="{{strYear}}"{{/isFirstInYear}}>
          <h2><time datetime="{{strYear}}-{{strMonth}}">{{year}}年{{month}}月</time></h2>

          <ul class="showcase">
      {{/isFirstInMonth}}

      <li>
        {{{body}}}
        <span>{{title}}</span>
        <span>{{description}}</span>
      </li>

      {{#isLastInMonth}}
          </ul>
        </section>
      {{/isLastInMonth}}
    {{/books}}

    {{> page-footer}}
  </body>
</html>
